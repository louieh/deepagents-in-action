---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="flex items-center gap-3 mb-8">
  <div class="w-1 h-6 bg-gold rounded-full"></div>
  <h2 class="text-sm font-mono font-medium tracking-widest text-ink-muted uppercase">
    {title}
  </h2>
</div>